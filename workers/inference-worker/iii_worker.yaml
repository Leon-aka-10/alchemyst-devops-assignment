name: math-worker          # must match InitOptions worker_name in inference_worker.py
runtime:
  language: python
  package_manager: pip
  entry: inference_worker.py
scripts:
  install: "pip install -r requirements.txt"
  start: "python inference_worker.py"   # removed watchfiles for production
resources:
  memory: 8192
  cpus: 4